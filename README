✅ Required Python Packages
Your script uses the following external libraries:

pandas

openpyxl

Pillow (for Image and ImageTk)

tkinter and datetime are part of the Python standard library (no install needed).


## Installation

Before running the application, make sure you have the required Python packages installed.

### Requirements

- Python 3.7 or higher
- pip (Python package installer)

### Install Dependencies

Run the following command in your terminal or command prompt:

```bash
pip install pandas openpyxl Pillow